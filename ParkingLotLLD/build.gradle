plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
}

application {
    // Make sure this matches your actual package + class name
    mainClass = 'org.example.Main'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}
